<UserControl x:Class="HeavyKeyProcessing.SomeControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:heavyKeyProcessing="clr-namespace:HeavyKeyProcessing"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance heavyKeyProcessing:SomeControlViewModel}">
    <StackPanel Margin="5">
        <TextBox Name="TextBox1" Margin="5" KeyDown="TextBox1_KeyDown" ></TextBox>
        <TextBlock Name="TextBlock1" Height="22" Margin="5" Background="AliceBlue"/>
        <StackPanel Height="100"></StackPanel>
        <TextBox Text="{Binding TestText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                 Margin="5" Height="22" />
        <TextBlock Height="22" Text="{Binding ResultText}" Margin="5" Background="AliceBlue"/>
    </StackPanel>
</UserControl>
