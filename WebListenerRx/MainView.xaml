<UserControl x:Class="WebListenerRx.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:webListenerRx="clr-namespace:WebListenerRx"
             mc:Ignorable="d" 
             Width="900" d:DataContext="{d:DesignData MainViewModel}">
    <DockPanel>
        <ListView ItemsSource="{Binding Rows}">
            <i:Interaction.Behaviors>
                <webListenerRx:ScrollToBottomListViewBehavior/>
                <webListenerRx:ScrollToBottomOnAddBehavior />
            </i:Interaction.Behaviors>
        </ListView>
    </DockPanel>
</UserControl>
