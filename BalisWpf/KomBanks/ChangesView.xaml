<Window x:Class="BalisWpf.ChangesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight" Topmost="True">
    <Grid>
        <ListView Margin="10" ItemsSource="{Binding Rows}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Bank}"></GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Timestamp}" Width="100"></GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=UsdRate}"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
