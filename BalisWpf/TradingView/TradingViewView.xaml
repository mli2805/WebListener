<UserControl x:Class="BalisWpf.TradingViewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
            >
   <DockPanel>
       <TextBlock Text="{Binding Title}" TextAlignment="Center" DockPanel.Dock="Top"/>
       <ListBox ItemsSource="{Binding LeftPanel}" BorderThickness="0" Width="150"/>
       <ListBox ItemsSource="{Binding RightPanel}" BorderThickness="0"/>
   </DockPanel>
</UserControl>
